[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = ""
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.volumes]]
mountPath = "/data"
name = "minecraft-data"